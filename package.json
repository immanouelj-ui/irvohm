{
  "name": "irv-ohm-backend",
  "version": "1.0.0",
  "description": "Backend paiement Stripe pour IRV'OHM",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "stripe": "^14.0.0",
    "nodemailer": "^6.9.0",
    "googleapis": "^140.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
